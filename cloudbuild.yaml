steps:
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    args:
      - gcloud
      - functions
      - deploy
      - get_all_accommodations
      - --region=europe-central2
      - --source=.
      - --trigger-http
      - --runtime=python39
